open Jest;
open Expect;
// open Parsing;

describe("parseLine", () => {
  test("push", ()
    // expect({|push "abc"|} |> parseLine) |> toEqual(Db.Push("abc"))
    =>
      expect(1) |> toBe(1)
    )
});
